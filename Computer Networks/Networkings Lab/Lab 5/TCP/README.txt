to make executables use make in this directory
The server receives image from the client in the format that the server file is given name

The client and server both read and write binaries--> "rb" and "wb" mode in fopen()

The latency is calculated by getting time difference sending and receiving, and is calculated by subtracting sending the time from sender and subtracting this from the time calculated at resceiver end.