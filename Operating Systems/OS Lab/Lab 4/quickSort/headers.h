#ifndef headers
#define headers

#include <stdio.h>
#include <stdlib.h>
#include <sys/wait.h>
#include <sys/types.h>

extern int qSort(int arr[], int right, int left, int pivot);
extern int randomfill(int arr[], int len);

#define limit 1000

#endif 